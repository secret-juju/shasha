// import { createSlice } from '@reduxjs/toolkit';

// import { StoreType } from '../../module/store';

// const sliceName = 'bookmark';
// const initialState = {};
// const reducers = {
//   subscribeCompany: (state = initialState, action) => {},
// };

// const bookmarkSlice = createSlice({ name: sliceName, initialState, reducers });

// const { actions, reducer } = bookmarkSlice;

// export const {} = actions;

// export const bookmarkSliceState = (state: StoreType) => state.bookmark;

// export default reducer;
